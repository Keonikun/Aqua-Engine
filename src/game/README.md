# Game

Game-specific code lives here.

`client` owns local presentation and camera-facing behavior.
`shared` owns reusable gameplay definitions such as movement constants and entity class names.

There is no multiplayer/server layer planned for the first milestone.
