# Client

Browser-facing app startup lives here.

This is roughly analogous to Source's client side entry layer: it owns DOM bootstrapping, canvas attachment, pointer-lock entry points, and handoff into the engine runtime.

Keep gameplay rules and collision out of this folder.
